namespace History.WindowsClient.ViewModels;

// Main window view model: hosts the window-level dialog and loading requests raised by flows
// that start outside a page, such as toast deep links.
public sealed class MainWindowViewModel : BaseViewModel { }
