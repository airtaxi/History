<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage
    x:Class="History.WindowsClient.Pages.Extras.InviteCodesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extras="using:History.WindowsClient.ViewModels.Extras"
    xmlns:local="using:History.WindowsClient.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="OnPageLoaded"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <local:BasePage.Resources>
        <DataTemplate x:Key="InviteCodeTemplate" x:DataType="extras:InviteCodeViewModel">
            <Border
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock
                            FontSize="24"
                            FontWeight="SemiBold"
                            Text="{x:Bind Code}"
                            TextTrimming="CharacterEllipsis" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind StatusText}" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind UsedByText}"
                            Visibility="{x:Bind IsUsed, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </StackPanel>

                    <Button
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        AutomationProperties.Name="초대 코드 복사"
                        Command="{x:Bind HandleCopyCommand}"
                        ToolTipService.ToolTip="초대 코드 복사"
                        Visibility="{x:Bind IsActive, Converter={StaticResource BoolToVisibilityConverter}}">
                        <FontIcon FontSize="18" Glyph="{x:Bind CopyButtonGlyph, Mode=OneWay}" />
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>
    </local:BasePage.Resources>

    <Grid Padding="16,0,16,16">
        <RefreshContainer RefreshRequested="OnRefreshRequested">
            <ScrollViewer VerticalScrollBarVisibility="Hidden">
                <ItemsRepeater
                    x:Name="MainItemsRepeater"
                    ElementPrepared="OnMainItemsRepeaterElementPrepared"
                    ItemTemplate="{StaticResource InviteCodeTemplate}"
                    ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="8" />
                    </ItemsRepeater.Layout>
                </ItemsRepeater>
            </ScrollViewer>
        </RefreshContainer>

        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="초대 코드가 없습니다"
            Visibility="{x:Bind ViewModel.IsEmpty, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
    </Grid>
</local:BasePage>
